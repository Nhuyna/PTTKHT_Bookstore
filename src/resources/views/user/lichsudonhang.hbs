
<div id="confirmModal" class="fixed inset-0 z-50 flex items-center justify-center bg-black bg-opacity-40 hidden">
  <div class="bg-white animate-fade-in w-full max-w-lg rounded-2xl p-6 shadow-xl relative">
    <button onclick="closeConfirmModal()" class="absolute top-4 right-4 text-gray-400 hover:text-gray-600 text-xl">&times;</button>

    <h2 id="modalTitle" class="text-xl font-bold text-gray-800 mb-4">Xác nhận trả đơn hàng</h2>
    <p id="modalMessage" class="text-gray-600 mb-4">Vui lòng cho chúng tôi biết lý do bạn muốn trả đơn hàng này</p>

    <form id="actionForm" class="space-y-4">
      <input type="hidden" name="order_id" value="">
      <textarea name="reason" required rows="4"
        class="w-full border border-gray-300 rounded-lg px-3 py-2 focus:outline-none focus:ring-2 focus:ring-red-400"
        placeholder="Nhập lý do trả đơn hàng...">
      </textarea>

      <div class="flex justify-end gap-3">
        <button type="button" onclick="closeConfirmModal()" 
          class="bg-gray-200 text-gray-700 px-4 py-2 rounded-lg hover:bg-gray-300 transition">
          Quay lại
        </button>
        <button type="submit"
          class="bg-red-500 btn-tra-hang text-white px-5 py-2 rounded-lg hover:bg-red-600 font-semibold transition">
          Xác nhận
        </button>
      </div>
    </form>
  </div>
</div>

<div id="cancelModal" class="fixed inset-0 z-50 flex items-center justify-center bg-black bg-opacity-40 hidden">
  <div class="bg-white animate-fade-in w-full max-w-lg rounded-2xl p-6 shadow-xl relative">
    <button  class="absolute top-4 right-4 text-gray-400 hover:text-gray-600 text-xl">&times;</button>

    <h2 class="text-xl font-bold text-gray-800 mb-4">Xác nhận hủy đơn hàng</h2>
    <p class="text-gray-600 mb-4">Vui lòng cho chúng tôi biết lý do bạn muốn hủy đơn hàng này.</p>

    <form  class="space-y-4">
      <input type="hidden" name="order_id" > 

      <textarea name="cancel_reason" required rows="4"
        class="w-full border border-gray-300 rounded-lg px-3 py-2 focus:outline-none focus:ring-2 focus:ring-red-400"
        placeholder="Nhập lý do hủy đơn hàng..."></textarea>

      <div class="flex justify-end gap-3">
        <button type="button" 
          class="bg-gray-200 text-gray-700 px-4 py-2 rounded-lg hover:bg-gray-300 transition">
          Quay lại
        </button>
        <button type="submit"
          class= " bg-red-500 text-white px-5 py-2 rounded-lg hover:bg-red-600 font-semibold transition">
          Xác nhận hủy
        </button>
      </div>
    </form>
  </div>
</div>


<div class="bg-gray-50 p-4 min-h-screen">
  <!-- Tabs -->
  <div class="max-w-4xl mx-auto mb-4">
    <div class="flex justify-between border-b">
      <a href="/lichsudonhang" class="tab-button px-4 py-2 ">
        Tất cả
      </a>

      <a href="/lichsudonhang?status=Chờ xác nhận" class="tab-button px-4 py-2 ">
        Chờ giao hàng
      </a>

      <a href="/lichsudonhang?status=Đã giao" class="tab-button px-4 py-2 ">
        Hoàn thành
      </a>

      <a href="/lichsudonhang?status=Đã hủy" class="tab-button px-4 py-2 ">
        Đã huỷ
      </a>

      <a href="/lichsudonhang?status=Trả hàng" class="tab-button px-4 py-2 ">
        Trả hàng/Hoàn tiền
      </a>
    </div>
    <!-- Search bar -->
    <div class="mt-2 bg-gray-100 rounded flex items-center px-4 py-2">
      <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 text-gray-500 mr-2" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-4.35-4.35m0 0A7.5 7.5 0 1110.5 3a7.5 7.5 0 016.15 13.65z" />
      </svg>
      <input type="text" placeholder="Bạn có thể tìm kiếm theo tên Shop, ID đơn hàng hoặc Tên Sản phẩm" class="w-full bg-gray-100 focus:outline-none text-sm text-gray-700">
    </div>
  </div>

  {{#each history}}
  <div class="max-w-4xl mx-auto bg-white shadow-lg rounded-xl p-4 mt-4">
    <div class="bg-gray-50 px-4 py-2 rounded-md shadow-sm text-gray-700 text-lg inline-block mb-2">
      📅 Ngày đặt hàng: {{NgayXuat}}
    </div>


    <div class="flex justify-between items-center  mb-4">
      <div class="flex items-center gap-3">
        {{!-- <button class="bg-red-500 text-white text-sm px-3 py-1 rounded hover:bg-red-600">💬 Chat</button> --}}
        <a href="/" class="border border-gray-300 text-base px-3 py-1 rounded hover:bg-gray-100 inline-block">
          🏪 Xem Shop
        </a>

      </div>

      <div class="flex items-center gap-3">
        <span class="text-green-500 flex items-center gap-1">
          <span class="{{TrangThaiColor}}">{{TrangThaiText}}</span>

        </span>
        {{!-- <span class="text-gray-400">|</span>
        <span class="text-yellow-500 font-bold uppercase">HOÀN THÀNH </span> --}}
      </div>
    </div>


    {{#each ChiTietHoaDonXuat}}
      <div class="flex justify-between  pb-4 mb-4 border-t pt-4 mt-4">
        <div class="flex items-start gap-4">
          <img src="{{this.Anh}}" alt="math" class="w-24 h-24 object-cover rounded border">
          <div>
            <h2 class="font-semibold text-gray-700">Tên : {{this.TenSanPham}}</h2>
            <p class="text-red-500 font-semibold mt-2">Giá : {{this.Gia}}</p>
            <p class="font-semibold mt-2">Số lượng : {{this.SoLuong}}</p>
          </div>
        </div>

        <div class="flex flex-col items-end justify-between">
          <div>
            <span class="text-lg font-medium text-gray-700">Thành tiền:</span>
            <span class="text-xl font-bold text-red-600 k">{{this.ThanhTien}}</span>
          </div>
          <div class="flex gap-2 mt-4">
            <form  style="display: inline;">
              <button type="button" data-product-id="{{IDSanPham}}" class="bg-red-500 add-to-cart hover:bg-red-600 text-white px-4 py-2 rounded-xl font-medium ">
                Mua Lại
              </button>
            </form>


          </div>
        </div>
      </div>

    {{/each}}
    <div class="flex flex-col items-start justify-between border-t pt-4 mt-4">
      <div>
        <span class="text-lg font-medium text-gray-700 mr-4">Tổng :</span>
        <span class="text-xl font-bold text-red-600 ">{{this.TongTien}}</span>
      </div>
        {{#if DaHuy}}
          <form >
            <input type="hidden"  name="IDHoaDonXuat" value="{{../IDHoaDonXuat}}">
            
            <button onclick="openCancelModal(this)"  data-id="{{IDHoaDonXuat}}" type="button" class=" text-gray-800 border border-gray-300 px-6 py-2 rounded-lg hover:bg-gray-100 transition">
              Hủy
            </button>
          </form>
        {{/if}}
        {{#if HienTraHang}}
          <form>
            <input type="hidden" name="IDHoaDonXuat" value="{{IDHoaDonXuat}}">
            <button 
            onclick="openConfirmModal(this)"
              data-id="{{IDHoaDonXuat}}" 
              type="button" 
              class=" text-gray-800 border border-gray-300 px-6 py-2 rounded-lg hover:bg-gray-100 transition {{TraHangClass}}"
              {{#unless TraHang}}disabled{{/unless}}
            >
              Trả hàng
            </button>
          </form>
        {{/if}}

      </div>
  </div>
  

  {{/each}}

  <script>
    function formatCurrencyVND(amount) {
      if (typeof amount !== 'number') {
        amount = parseFloat(amount);
      }

      return amount.toLocaleString('vi-VN', {
        maximumFractionDigits: 0
      }) + ' đ';
    }

    document.addEventListener("DOMContentLoaded", function () {
      const tabs = document.querySelectorAll(".tab-button");
      const activeClass = "text-red-500 border-b-2 border-red-500 font-medium";

      tabs.forEach(tab => {
        tab.addEventListener("click", function () {
          tabs.forEach(t => t.classList.remove(...activeClass.split(" ")));
          this.classList.add(...activeClass.split(" "));
        });
      });
    });
  </script>
  <script>
  document.querySelectorAll('.add-to-cart').forEach(button => {
    button.addEventListener('click', async () => {
      const productId = button.getAttribute('data-product-id');

      try {
        const response = await fetch('/cart/addToCart', {
          method: 'POST',
          headers: {
            'Content-Type': 'application/json'
          },
          body: JSON.stringify({ productId })
        });

        const result = await response.json();

        alert(result.message);

        if (result.redirect) {
          window.location.href = result.redirect;
        }
      } catch (error) {
        alert("Đã xảy ra lỗi khi thêm vào giỏ hàng.");
        console.error(error);
      }
    });
  });
</script>
<script>
  document.addEventListener('DOMContentLoaded', function () {
  const huyForm = document.querySelector('#cancelModal form');

  huyForm.addEventListener('submit', async function (e) {
    e.preventDefault();

    const orderId = this.querySelector('input[name="order_id"]').value;
    const reason = this.querySelector('textarea[name="cancel_reason"]').value;

    if (!reason.trim()) {
      alert("Vui lòng nhập lý do hủy đơn hàng.");
      return;
    }

    try {
      const response = await fetch("/order/huyDonHang", {
        method: "POST",
        headers: {
          "Content-Type": "application/json",
        },
        body: JSON.stringify({
          IDHoaDonXuat: orderId,
          cancel_reason: reason
        }),
      });

      const result = await response.json();

      if (result.success) {
        alert("Đã hủy đơn hàng.");
        location.reload();
      } else {
        alert("Hủy đơn hàng thất bại: " + result.message);
      }
    } catch (error) {
      console.error("Lỗi khi gửi yêu cầu hủy:", error);
      alert("Lỗi khi gửi yêu cầu hủy đơn hàng.");
    }
  });
});

</script>
<script>
function openConfirmModal(button) {
  const orderId = button.getAttribute('data-id');
  const modal = document.getElementById('confirmModal');

  modal.querySelector('input[name="order_id"]').value = orderId;

  modal.classList.remove('hidden', 'fade-out');
  modal.classList.add('fade-in');
}

function closeConfirmModal() {
  const modal = document.getElementById('confirmModal');
  modal.classList.remove('fade-in');
  modal.classList.add('fade-out');

  setTimeout(() => {
    modal.classList.add('hidden');
  }, 200);
}

document.getElementById('actionForm').addEventListener('submit', async function (e) {
  e.preventDefault();

  const orderId = this.querySelector('input[name="order_id"]').value;
  const reason = this.querySelector('textarea[name="reason"]').value.trim();

  if (!reason) {
    alert("Vui lòng nhập lý do trả hàng.");
    return;
  }

  try {
    const response = await fetch("/order/TraHang", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ IDHoaDonXuat: orderId, reason }),
    });

    const result = await response.json();

    if (result.success) {
      alert("Yêu cầu trả hàng thành công.");
      location.reload();
    } else {
      alert("Yêu cầu trả hàng thất bại: " + result.message);
    }
  } catch (error) {
    console.error("Lỗi khi gửi yêu cầu trả hàng:", error);
    alert("Đã xảy ra lỗi khi gửi yêu cầu.");
  }
});
</script>

</div>
<script>
function openCancelModal(button) {
  const orderId = button.getAttribute('data-id');
  document.querySelector('#cancelModal input[name="order_id"]').value = orderId;
  document.getElementById('cancelModal').classList.remove('hidden');
}

  function closeCancelModal() {
    document.getElementById('cancelModal').classList.add('hidden');
  }
</script>

